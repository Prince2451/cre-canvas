const queryKeys = {
  user: "User",
};

const apiUrls = {
  auth: {
    refreshToken: "auth/token",
    login: "auth/login",
    register: "auth/register",
    user: "auth/user",
  },
};

export { apiUrls, queryKeys };
